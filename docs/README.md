<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

-   [Point][1]
    -   [Parameters][2]
    -   [distance][3]
        -   [Parameters][4]
    -   [distance][5]
        -   [Parameters][6]
    -   [distance][7]
        -   [Parameters][8]
    -   [distance][9]
        -   [Parameters][10]
    -   [distance][11]
        -   [Parameters][12]
    -   [toString][13]
    -   [clone][14]
    -   [distance][15]
        -   [Parameters][16]
    -   [fromUTM][17]
        -   [Parameters][18]
-   [hemisphere][19]
    -   [NORTH][20]
    -   [SOUTH][21]
-   [tap][22]
    -   [Parameters][23]
-   [retry][24]
    -   [Parameters][25]
-   [multicast][26]
-   [share][27]
-   [filter][28]
    -   [Parameters][29]
-   [merge][30]
    -   [Parameters][31]
-   [map][32]
    -   [Parameters][33]
-   [debounce][34]
    -   [Parameters][35]
-   [throttle][36]
    -   [Parameters][37]
-   [delay][38]
    -   [Parameters][39]
-   [skip][40]
    -   [Parameters][41]
-   [skipWhile][42]
    -   [Parameters][43]
-   [skipUntil][44]
    -   [Parameters][45]
-   [take][46]
    -   [Parameters][47]
-   [takeWhile][48]
    -   [Parameters][49]
-   [takeUntil][50]
    -   [Parameters][51]
-   [mergeMap][52]
    -   [Parameters][53]
-   [flatMap][54]
    -   [Parameters][55]
-   [concatMap][56]
    -   [Parameters][57]
-   [tumblingCountWindow][58]
    -   [Parameters][59]
-   [slidingCountWindow][60]
    -   [Parameters][61]
-   [tumblingTimeWindow][62]
    -   [Parameters][63]
-   [slidingTimeWindow][64]
    -   [Parameters][65]
-   [fixedIntervalWindow][66]
    -   [Parameters][67]
-   [eventIntervalWindow][68]
    -   [Parameters][69]
-   [groupBy][70]
    -   [Parameters][71]
-   [project][72]
    -   [Parameters][73]
-   [all][74]
    -   [Parameters][75]
-   [any][76]
    -   [Parameters][77]
-   [abscence][78]
    -   [Parameters][79]
-   [minDistance][80]
    -   [Parameters][81]
-   [maxDistance][82]
    -   [Parameters][83]
-   [avgDistance][84]
    -   [Parameters][85]
-   [relativeMinDistance][86]
    -   [Parameters][87]
-   [relativeMaxDistance][88]
    -   [Parameters][89]
-   [relativeAvgDistance][90]
    -   [Parameters][91]
-   [always][92]
    -   [Parameters][93]
-   [sometimes][94]
    -   [Parameters][95]
-   [nHighestValues][96]
    -   [Parameters][97]
-   [nLowestValues][98]
    -   [Parameters][99]
-   [count][100]
    -   [Parameters][101]
-   [valueMax][102]
    -   [Parameters][103]
-   [valueMin][104]
    -   [Parameters][105]
-   [valueAvg][106]
    -   [Parameters][107]
-   [increasing][108]
    -   [Parameters][109]
-   [decreasing][110]
    -   [Parameters][111]
-   [stable][112]
    -   [Parameters][113]
-   [nonIncreasing][114]
    -   [Parameters][115]
-   [nonDecreasing][116]
    -   [Parameters][117]
-   [mixed][118]
    -   [Parameters][119]
-   [of][120]
    -   [Parameters][121]
-   [from][122]
    -   [Parameters][123]
-   [fromEvent][124]
    -   [Parameters][125]
-   [interval][126]
    -   [Parameters][127]
-   [timer][128]
    -   [Parameters][129]
-   [orderPolicy][130]
    -   [OCCURRENCE_TIME][131]
    -   [DETECTION_TIME][132]
    -   [STREAM_POSITION][133]
-   [recurrence][134]
    -   [NONE][135]
    -   [DAYLY][136]
    -   [WEEKLY][137]
    -   [MONTHLY][138]
    -   [YEARLY][139]
-   [ordering][140]
    -   [OCCURRENCE_TIME][141]
    -   [DETECTION_TIME][142]
-   [EventStream][143]
    -   [pipe][144]
        -   [Parameters][145]
    -   [subscribe][146]
        -   [Parameters][147]
-   [StreamSubscription][148]

## Point

Class used to represent a given point location.

### Parameters

-   `latitude` **[number][149]** The latitude value.
-   `longitude` **[number][149]** The longitude value.

### distance

Get the longitude value.

#### Parameters

-   `point2`  
-   `fixed`  

Returns **[number][149]** The longitude value.

### distance

Calculate the distance between this instance and a second point.

#### Parameters

-   `point2` **[Point][150]** the second point.
-   `fixed` **[number][149]** the precision of the result.

Returns **[number][149]** the distance in meters (metres).

### distance

Set the longitude value.

#### Parameters

-   `point2`  
-   `fixed`  
-   `longitude` **[number][149]** the longitude value to be set.

### distance

Set the longitude value.

#### Parameters

-   `point2`  
-   `fixed`  
-   `latitude` **[number][149]** the latitude value to be set.

### distance

Get the latitude value.

#### Parameters

-   `point2`  
-   `fixed`  

Returns **[number][149]** The latitude value.

### toString

Returns a string representing the point location.

Returns **[string][151]** a string representing the current object.

### clone

Clones the current object.

Returns **[Point][150]** a cloned new object.

### distance

Calculate the distance between two given points.

#### Parameters

-   `point1` **[Point][150]** the first point.
-   `point2` **[Point][150]** the second point.
-   `fixed` **[number][149]** the precision of the result.

Returns **[number][149]** the distance in meters (metres).

### fromUTM

Instatiate a point from UTM coordinates.

#### Parameters

-   `zone` **[number][149]** the UTM zone.
-   `hemisphere` **[string][151]** the hemisphere north or south. Use [hemisphere.NORTH][152] or [hemisphere.SOUTH][153].
-   `easting` **[number][149]** the easting coordinate.
-   `northing` **[number][149]** the northing coordinate.

Returns **[Point][150]** a point instance.

## hemisphere

Enum for representing the hemisphere north and south

### NORTH

Constant representing the hemisphere north

### SOUTH

Constant representing the hemisphere south.

## tap

### Parameters

-   `fn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## retry

### Parameters

-   `count` **[number][149]** 

Returns **[EventStream][155]** a new event stream instance.

## multicast

Returns **[EventStream][155]** a new event stream instance.

## share

Returns **[EventStream][155]** a new event stream instance.

## filter

### Parameters

-   `fn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## merge

### Parameters

-   `eventStreams` **...[EventStream][155]** 

Returns **[EventStream][155]** the new stream created from the merge operation.

## map

### Parameters

-   `projectionFn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## debounce

### Parameters

-   `timeDuration` **[number][149]** 

Returns **[EventStream][155]** a new event stream instance.

## throttle

### Parameters

-   `timeDuration` **[number][149]** 

Returns **[EventStream][155]** a new event stream instance.

## delay

Delay the event stream's emission.

### Parameters

-   `timeDelay` **[number][149]** 

Returns **[EventStream][155]** a new event stream instance.

## skip

Skip a given number of events.

### Parameters

-   `count` **[number][149]** the number of events to be skipped.

Returns **[EventStream][155]** a new event stream instance.

## skipWhile

Skip events while the given predicate doesn't return false.

### Parameters

-   `predicate` **[Function][154]** a predicate that controls events' emission.

Returns **[EventStream][155]** a new event stream instance.

## skipUntil

Skip events until the provided event stream smits.

### Parameters

-   `outerEventStream` **[EventStream][155]** some event stream instance.

Returns **[EventStream][155]** a new event stream instance.

## take

Take only the provided number of events and then completes.

### Parameters

-   `count` **[number][149]** the number of events to be taken.

Returns **[EventStream][155]** a new event stream instance.

## takeWhile

Take events while the given predicate doesn't return false.

### Parameters

-   `predicate` **[Function][154]** a predicate that controls events' emission.

Returns **[EventStream][155]** a new event stream instance.

## takeUntil

Take events until the provided event stream smits.

### Parameters

-   `outerEventStream` **[EventStream][155]** some event stream instance.

Returns **[EventStream][155]** a new event stream instance.

## mergeMap

### Parameters

-   `projectionFn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## flatMap

### Parameters

-   `projectionFn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## concatMap

### Parameters

-   `projectionFn` **[Function][154]** 

Returns **[EventStream][155]** a new event stream instance.

## tumblingCountWindow

Splits the stream into sets of fixed size.

### Parameters

-   `size` **[number][149]** the window size.

Returns **[EventStream][155]** a new event stream instance.

## slidingCountWindow

Splits the stream into sets of fixed size. Additionally, it receives a second parameter 
that control when a new window must be open. If the slidingInterval is less than the window size, it will
produce overlapping sets.

### Parameters

-   `size` **[number][149]** the window size.
-   `slidingInterval`   (optional, default `1`)
-   `slidingInterval-null` **[number][149]** the interval at which a new window must be open. It defaults to 1.

Returns **[EventStream][155]** a new event stream instance.

## tumblingTimeWindow

Splits the stream into sets of time-based fixed size.

### Parameters

-   `timeSize` **[number][149]** the window size in milliseconds.

Returns **[EventStream][155]** a new event stream instance.

## slidingTimeWindow

Splits the stream into sets of time-based fixed size. Additionally, it receives a second parameter 
that control when a new window must be open. If the slidingInterval is less than the timeSize, it will
produce overlapping sets.

### Parameters

-   `timeSize` **[number][149]** the window size in milliseconds.
-   `slidingInterval` **[number][149]** the interval (milliseconds) at which a new window must be open.

Returns **[EventStream][155]** a new event stream instance.

## fixedIntervalWindow

### Parameters

-   `start` **[date][156]** 
-   `end` **([date][156] \| [number][149])** 
-   `recurrence` **[recurrence][157]** 
-   `order` **[ordering][158]** 

Returns **[EventStream][155]** a new event stream instance.

## eventIntervalWindow

### Parameters

-   `initiatorEvents` **[Array][159]&lt;[string][151]>** a list containing the event types ids of the instances that initiates the window.
-   `terminatorEvents` **[Array][159]&lt;[string][151]>** a list containing the event types ids of the instances that close the window.
-   `expirationTime` **[number][149]?** an optional time period used to close the window in case a terminator event
    has not been detected so far.

Returns **[EventStream][155]** a new event stream instance.

## groupBy

### Parameters

-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the given event attribute to be considered. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.

Returns **[EventStream][155]** a new event stream instance.

## project

### Parameters

-   `attributeList` **[Array][159]&lt;[string][151]>** 
-   `newEventTypeId` **[string][151]** 

Returns **[EventStream][155]** a new event stream instance.

## all

Based on the logical conjunction operation, this pattern looks for one instance of each event type ids listed on {@code eventTypeList}.
Optionally, the pattern accepts an assertion that can be used to define a condition that the matching set should meet.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.
-   `assertion` **[Function][154]?** an assertion to be tested against the matching set.

Returns **[EventStream][155]** a new event stream instance.

## any

Based on the logical disjunction operation, this pattern looks for one event instance of any of event type ids listed on {@code eventTypeList}.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## abscence

Based on the logical negation operation, this pattern is satisfied when there are no event instances listed on {@code eventTypeList}.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## minDistance

This pattern is matched when the minimal distance of the events' locations from a given point satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `givenPoint` **[Point][150]** a fixed point location used to calculate the absolute distance.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance between the events' locations and the given point. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the minimal distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## maxDistance

This pattern is matched when the maximal distance of the events' locations from a given point satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `givenPoint` **[Point][150]** a fixed point location used to calculate the absolute distance.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance between the events' locations and the given point. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute
-   `assertion` **[Function][154]** a threshold assertion to be used against the maximal distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## avgDistance

This pattern is matched when the average distance of the events' locations from a given point satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `givenPoint` **[Point][150]** a fixed point location used to calculate the absolute distance.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance between the events' locations and the given point. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the average distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## relativeMinDistance

This pattern is matched when the minimal distance among the events' locations satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance among the events' locations. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the relative minimal distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## relativeMaxDistance

This pattern is matched when the maximal distance among the events' locations satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance among the events' locations. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the relative maximal distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## relativeAvgDistance

This pattern is matched when the average distance among the events' locations satisfies a given threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the common attribute carried by all event occurrences that is used to
    calculate the distance among the events' locations. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the relative average distance.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## always

This pattern is satisfied when all participant events match a given assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `assertion` **[Function][154]** an assertion to be tested against all the participant events.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## sometimes

This pattern is satisfied when at least one participant event matches a given assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `assertion` **[Function][154]** an assertion to be tested against all the participant events.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## nHighestValues

This pattern selects a subset of the participant events with the n highest values of a given event attribute.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `n` **[number][149]** the number of events to be selected according to the highest value criteria.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the given event attribute to be considered. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## nLowestValues

This pattern selects a subset of the participant events with the n lowest values of a given event attribute.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `n` **[number][149]** the number of events to be selected according to the lowest value criteria.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the given event attribute to be considered. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## count

This pattern counts the number of the participant events (those events whose ids are listed on eventTypeList) and
tests an assertion against this value. This operation works on chunks of the stream, so it must be preceded by some
window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `assertion` **[Function][154]** a threshold assertion to be used against the number of instances.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## valueMax

This pattern selects an event attribute in every participant events and tests its maximal value against a threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the maximal value of the given attribute.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## valueMin

This pattern selects an event attribute in every participant events and tests its minimal value against a threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the minimal value of the given attribute.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## valueAvg

This pattern selects an event attribute in every participant events and tests its average value against a threshold assertion.
This operation works on chunks of the stream, so it must be preceded by some window operation.

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `assertion` **[Function][154]** a threshold assertion to be used against the average value of the given attribute.
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## increasing

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## decreasing

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## stable

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## nonIncreasing

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## nonDecreasing

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## mixed

### Parameters

-   `eventTypeList` **[Array][159]&lt;[string][151]>** a list containing the event types that are to be considered in the pattern operation.
-   `attribute` **([string][151] \| [Array][159]&lt;([string][151] \| [number][149])>)** the instances' attribute to be examined. It can either be a simple string for a simple
    attribute or an array of strings and/or numbers in the case of a nesting structure indicating the path to the attribute.
-   `orderPolicy` **[orderPolicy][160]** 
-   `newEvtTypeId` **[string][151]** the event type id of new event generated when the pattern is satisfied.

Returns **[EventStream][155]** a new event stream instance.

## of

### Parameters

-   `values` **[Array][159]&lt;any>** values that are emitted in sequence.
-   `adaptor` **[Function][154]?** a function responsable for mapping the values emitted by the stream into instances
    of the EventType class or its subclasses.

Returns **[EventStream][155]** a new event stream instance.

## from

### Parameters

-   `source` **([Array][159] \| [Promise][161] | Iterable)** 
-   `adaptor` **[Function][154]?** a function responsable for mapping the values emitted by the stream into instances
    of the EventType class or its subclasses.

Returns **[EventStream][155]** a new event stream instance.

## fromEvent

### Parameters

-   `target` **EventTargetLike** 
-   `eventName` **[string][151]** 
-   `adaptor` **[Function][154]** adaptor is a function responsable
    for mapping the values emitted by the stream into instances of the EventType class or any EventType's subclass.

Returns **[EventStream][155]** a new event stream instance.

## interval

### Parameters

-   `period` **[number][149]** 
-   `adaptor` **[Function][154]?** a function responsable for mapping the values emitted by the stream into instances
    of the EventType class or its subclasses.

Returns **[EventStream][155]** a new event stream instance.

## timer

### Parameters

-   `initialDelay` **([number][149] \| [Date][156])** 
-   `optionalParameters` **[Object][162]** an object that can have two optional attributes: period and adaptor.
    period dictates how often subsequent values should be emitted. adaptor is a function responsable
    for mapping the values emitted by the stream into instances of the EventType class or any EventType's subclass. (optional, default `{}`)
    -   `optionalParameters.period`  
    -   `optionalParameters.adaptor`  

## orderPolicy

Enum for order policy.

### OCCURRENCE_TIME

Occurrence time policy

### DETECTION_TIME

Detection time policy

### STREAM_POSITION

Stream position policy

## recurrence

Enum for types of recurrence.

### NONE

No recurrence

### DAYLY

Dayly recurrence

### WEEKLY

Weekly recurrence

### MONTHLY

Monthly recurrence

### YEARLY

Yearly recurrence

## ordering

Enum for ordering options.

### OCCURRENCE_TIME

Occurrence time ordering

### DETECTION_TIME

Detection time ordering

## EventStream

Class representing an event stream.
It provides CEP functionalities by encapsulating and manipulating a given reactive stream.
All methods return a new stream instance.

### pipe

#### Parameters

-   `operations` **...any** 
-   `args` **[Array][159]&lt;[Function][154]>** 

Returns **[EventStream][155]** a new event stream instance.

### subscribe

Subscribes to an event stream.

#### Parameters

-   `observer` **[Object][162]** an [Observer][163] object containing at least a next method.

Returns **[StreamSubscription][164]** a StreamSubscription instance that allows further unsubscription.

## StreamSubscription

Class that encapsulates the underlying observable subscription.

[1]: #point

[2]: #parameters

[3]: #distance

[4]: #parameters-1

[5]: #distance-1

[6]: #parameters-2

[7]: #distance-2

[8]: #parameters-3

[9]: #distance-3

[10]: #parameters-4

[11]: #distance-4

[12]: #parameters-5

[13]: #tostring

[14]: #clone

[15]: #distance-5

[16]: #parameters-6

[17]: #fromutm

[18]: #parameters-7

[19]: #hemisphere

[20]: #north

[21]: #south

[22]: #tap

[23]: #parameters-8

[24]: #retry

[25]: #parameters-9

[26]: #multicast

[27]: #share

[28]: #filter

[29]: #parameters-10

[30]: #merge

[31]: #parameters-11

[32]: #map

[33]: #parameters-12

[34]: #debounce

[35]: #parameters-13

[36]: #throttle

[37]: #parameters-14

[38]: #delay

[39]: #parameters-15

[40]: #skip

[41]: #parameters-16

[42]: #skipwhile

[43]: #parameters-17

[44]: #skipuntil

[45]: #parameters-18

[46]: #take

[47]: #parameters-19

[48]: #takewhile

[49]: #parameters-20

[50]: #takeuntil

[51]: #parameters-21

[52]: #mergemap

[53]: #parameters-22

[54]: #flatmap

[55]: #parameters-23

[56]: #concatmap

[57]: #parameters-24

[58]: #tumblingcountwindow

[59]: #parameters-25

[60]: #slidingcountwindow

[61]: #parameters-26

[62]: #tumblingtimewindow

[63]: #parameters-27

[64]: #slidingtimewindow

[65]: #parameters-28

[66]: #fixedintervalwindow

[67]: #parameters-29

[68]: #eventintervalwindow

[69]: #parameters-30

[70]: #groupby

[71]: #parameters-31

[72]: #project

[73]: #parameters-32

[74]: #all

[75]: #parameters-33

[76]: #any

[77]: #parameters-34

[78]: #abscence

[79]: #parameters-35

[80]: #mindistance

[81]: #parameters-36

[82]: #maxdistance

[83]: #parameters-37

[84]: #avgdistance

[85]: #parameters-38

[86]: #relativemindistance

[87]: #parameters-39

[88]: #relativemaxdistance

[89]: #parameters-40

[90]: #relativeavgdistance

[91]: #parameters-41

[92]: #always

[93]: #parameters-42

[94]: #sometimes

[95]: #parameters-43

[96]: #nhighestvalues

[97]: #parameters-44

[98]: #nlowestvalues

[99]: #parameters-45

[100]: #count

[101]: #parameters-46

[102]: #valuemax

[103]: #parameters-47

[104]: #valuemin

[105]: #parameters-48

[106]: #valueavg

[107]: #parameters-49

[108]: #increasing

[109]: #parameters-50

[110]: #decreasing

[111]: #parameters-51

[112]: #stable

[113]: #parameters-52

[114]: #nonincreasing

[115]: #parameters-53

[116]: #nondecreasing

[117]: #parameters-54

[118]: #mixed

[119]: #parameters-55

[120]: #of

[121]: #parameters-56

[122]: #from

[123]: #parameters-57

[124]: #fromevent

[125]: #parameters-58

[126]: #interval

[127]: #parameters-59

[128]: #timer

[129]: #parameters-60

[130]: #orderpolicy

[131]: #occurrence_time

[132]: #detection_time

[133]: #stream_position

[134]: #recurrence

[135]: #none

[136]: #dayly

[137]: #weekly

[138]: #monthly

[139]: #yearly

[140]: #ordering

[141]: #occurrence_time-1

[142]: #detection_time-1

[143]: #eventstream

[144]: #pipe

[145]: #parameters-61

[146]: #subscribe

[147]: #parameters-62

[148]: #streamsubscription

[149]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[150]: #point

[151]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[152]: #hemispherenorth

[153]: #hemispheresouth

[154]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function

[155]: #eventstream

[156]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Date

[157]: #recurrence

[158]: #ordering

[159]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[160]: #orderpolicy

[161]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise

[162]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[163]: http://reactivex.io/rxjs/class/es6/MiscJSDoc.js~ObserverDoc.html

[164]: #streamsubscription
